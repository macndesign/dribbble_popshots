language: node_js
language: node_js
node_js:
  - "0.8"
  - "0.10"
before_script:
#  - npm install -g grunt-cli
  - npm install -g bower
#  - npm install -g gulp
  - npm install phantomjs
  - npm install -g karma
  - npm install -g karma-cli
  - bower install
#  - gulp build
#  - gulp style
